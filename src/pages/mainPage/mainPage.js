import React from 'react';
import Headers from '../../components/headers/headers';

export const MainPage = (props) => {
    return (
        <div>
            Main Page
            <Headers/>
        </div>
    );
}